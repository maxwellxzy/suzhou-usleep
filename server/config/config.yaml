server:
  port: 8080
  mode: debug  # debug / release

jwt:
  secret: "usleep-jwt-secret-key-change-in-production"
  expire_hours: 24

database:
  path: "./data/usleep.db"

admin:
  username: "admin"
  password: "admin123"  # 首次启动后请立即修改
